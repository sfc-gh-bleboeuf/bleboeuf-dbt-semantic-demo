models:
  - name: metricflow_time_spine
    description: "Standard time spine model required by MetricFlow for time-based calculations"
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Date column used as the MetricFlow time spine at DAY granularity"
